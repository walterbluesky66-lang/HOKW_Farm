# TODO

## 近期迭代

- 校验拆分后页面在主流浏览器中的本地打开体验。
- 校验兴趣圈经验记录页在本地打开、日期切换、历史修改、复制 TAG 和窄屏显示下的体验。
- 梳理 `app.js` 内部函数分区，在不改变行为的前提下增加更清晰的注释和模块边界。
- 为浇水规则补充轻量测试用例，重点覆盖 3 分钟测试作物、首次自浇、后续自浇区间、好友浇水上限和成熟判断。
- 为牧场规则补充轻量测试用例，重点覆盖 16/20 小时动物的 3/4 催产时间、动物摊倍率、默认半价回收、回本周期、数量联动和批次收获重置。
- 继续为一键规划补充轻量测试用例；当前已覆盖 7 天窗口过期刷新、Lv.52 高等级候选（含浣溪圆茄）和周五 08:00 双倍边界下的重型规划性能，后续重点覆盖总览时间进度、当前作物进度、三级详情箭头链、圆环目标进度、等待关键窗口、睡眠桥接、周末目标达成后转经验、等级解锁后重排。

## 功能增强

- 增加历史记录，记录每轮种植、成熟、收获和浇水行为。
- 支持多块独立菜地，允许不同作物并行管理。
- 如后续确认需要，再重新设计药物作物功能入口，独立计算成熟提醒、材料库存和采集计划。
- 为只读标准作物库补充维护校验，确保 `BUILT_IN_CROP_ARCHIVE` 的等级、产量、满级基准单价和单块基准收益一致。
- 增强动物档案库导入能力，把外部整理好的动物标准库直接导入网页。
- 后续可评估是否把牧场批次纳入更长期的收益规划，但不要混入现有一键种植排期。
- 增强备份导出体验，例如增加导入前差异预览，避免误覆盖当前种植记录、档案库和收益对比数据。
- 一键规划后续可扩展为多块田分别排期、手动标记已完成计划项、导出本周计划。
- 兴趣圈后续可增加真实每日祈福或活动备注；保持它与农场/牧场数据分离。

## 在线化方向

- 云存档第一版已采用 Supabase JSON 快照：后续需要用真实项目完成邮箱验证码、RLS 隔离、首次本地初始化云端和多浏览器恢复验证。
- 后续可增加云端/本地差异预览、恢复副本下载入口和同步失败重试历史。
- 设计更完整的离线可用策略：无网络时继续使用本地数据，恢复网络后提示是否覆盖或上传。
- 评估是否继续使用 JSON 快照同步，还是拆成更细的作物、动物、兴趣圈表。

## 质量保障

- 固化当前历史 BUG 的回归测试：
  - 3 分钟测试作物不能被成熟时间分钟取整拉长。
  - 进度条必须显示有效成长进度。
  - 浏览器通知按钮必须能触发权限申请和状态更新。
  - 页面内提示音保持柔和，不使用尖锐高频提示。
- 在移动端窄屏下检查按钮、卡片和进度信息是否溢出。
- 增加版本号或变更记录，方便用户区分本地旧版本和在线新版本。
